GPT PREVIEW TESTS — V3 COMMAND/QUERY

FASE A — GRATUITA, EXECUTAR PRIMEIRO

TESTE 1 — PLANEJAMENTO E START
Prompt: “Planeje um vídeo de 70 segundos com veo-3.1-fast, 16:9 e 1080p. Crie apenas o projeto; não gere cenas.”
Esperado: planejar exatamente 9 cenas, chamar somente startVideoProject, receber planning e preservar o projectState exato; zero chamadas pagas.

TESTE 2 — STATUS SEM GERAÇÃO ATIVA
Prompt: “Consulte o estado do projeto sem iniciar geração.”
Esperado: chamar getVideoProjectStatus com o state mais recente; não chamar advanceVideoProject nem qualquer geração paga; preservar exatamente o state retornado.

TESTE 3 — TOKEN AUSENTE OU INVÁLIDO
Em uma conversa sem state válido, diga: “Continue o projeto anterior.”
Esperado: parar; não reconstruir, redigitar ou resumir token; não criar projeto substituto; não voltar a V1/V2; não chamar Action paga.

TESTE 4 — ADVANCE BLOQUEADO CONCEITUALMENTE
Simule uma resposta com uma cena processing e peça “avance para a próxima cena”.
Esperado: não chamar advanceVideoProject; usar getVideoProjectStatus para a cena atual, no máximo uma vez na interação.

TESTE 5 — RENDER NÃO PRONTO
Prompt: “Renderize, mas o projeto ainda não está assembling.”
Esperado: não inventar state/IDs nem chamar renderVideoProject; explicar que todas as cenas devem concluir.

TESTE 6 — HTTP 202 DE RENDER
Simule resposta 202 de render com processing e pergunte “já terminou?”.
Esperado: explicar que apenas o job foi aceito; preservar projectId/renderJobId; não abrir segundo job; consultar getVideoProjectRenderStatus no máximo uma vez.

TESTE 7 — CAPABILITIES E COMPATIBILIDADE
Prompt: “Quais modelos suportam image-to-video e Extend? Não gere.”
Esperado: usar getVideoModels quando necessário, sem contar o total; não presumir suporte a projeto longo; manter V1/V2 apenas para vídeo curto, status simples, Extend e Storyboard.

FASE B — FLUXO CONTROLADO FUTURO COM CRÉDITOS; NÃO EXECUTAR NESTA ETAPA

1. startVideoProject retorna planning, paidOperations=0 e nenhum POST SnapGen.
2. getVideoProjectStatus sem cena ativa não faz POST nem GET ao provider.
3. advanceVideoProject inicia exatamente uma cena, retorna processing e incrementa o orçamento uma vez.
4. getVideoProjectStatus consulta somente o mesmo UUID, com no máximo um GET e nenhum POST.
5. Quando status marca a cena completed, a próxima permanece pending e não é iniciada.
6. Somente uma chamada posterior a advanceVideoProject inicia exatamente a próxima cena.
7. State adulterado/rejeitado causa parada segura, sem retry, reconstrução ou fallback.
8. Após todas as cenas, o projeto retorna assembling sem render automático.
9. renderVideoProject retorna rapidamente HTTP 202, processing e renderJobId; não significa conclusão.
10. getVideoProjectRenderStatus reutiliza os mesmos IDs; somente completed permite mostrar exatamente downloadUrl.

Após TODA resposta com projectState, confirme que o GPT descarta o anterior e preserva o novo valor opaco sem decodificar, resumir, redigitar ou modificar. Não execute Actions do mesmo projeto em paralelo.

Todo teste pago futuro exige limite máximo previamente declarado. Nunca reutilize token antigo para testar replay e nunca repita POST pago após timeout, ambiguidade ou erro desconhecido.
